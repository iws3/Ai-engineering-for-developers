let count: number = 10;
const message: string = "Hello";
const isValid: boolean = true;

// ANSWER TO EXERCISE 1
// The type annotations are being removed
// They only exist during compilation. Once compiled, it's gone