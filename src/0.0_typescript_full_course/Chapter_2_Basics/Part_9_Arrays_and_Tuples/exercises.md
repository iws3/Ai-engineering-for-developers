# Part 9: Arrays and Tuples — Exercises

## Exercise 1: Type Arrays (Beginner)
Create typed arrays:

```typescript
let ___ = [1, 2, 3];
let ___ = ["apple", "banana"];
let ___ = [true, false];
```

---

## Exercise 2: Tuples (Intermediate)
Create tuple types:

```typescript
type Pair = [___, ___];
type Color = [number, number, number];

const pair: Pair = [10, 20];
const rgb: Color = [255, 128, 0];
```

---

## Exercise 3: Array Methods (Intermediate)
Use array methods with type safety:

```typescript
const nums: number[] = [1, 2, 3];
nums.map(x => x * 2);
nums.filter(x => x > 2);
```

---

## 📝 Submission
Show array and tuple examples with type annotations
